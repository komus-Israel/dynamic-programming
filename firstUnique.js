function firstUnique(array, memo={}) {

    // return the only elemnt present in the array
    if (array.length === 0) {
        return array[0]
    }

    

}


array = [10]
console.log(firstUnique(array))